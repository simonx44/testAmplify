{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "test": "jest --roots __tests__/unit",
        "integ-test": "jest --roots __tests__/integration",
        "describe": "aws cloudformation describe-stacks --region eu-central-1 --stack-name app --query 'Stacks[].Outputs'"
    },
    "dependencies": {
        "aws-sdk": "^2.893.0",
        "uuid": "^8.3.2"
    }
}
